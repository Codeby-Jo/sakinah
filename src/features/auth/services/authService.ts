export const authService = {
  signIn: async (email: string, password: string) => null,
  signUp: async (email: string, password: string, name?: string) => null,
  signOut: async () => {},
  sendPasswordReset: async (email: string) => {},
};
